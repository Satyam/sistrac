const C = require('../constants');
describe('constants usuarios', () => {
  it('', () => {
    // No longer valid since I moved into it other constants
    // besides action types constants, which don't follow their rules
    //   const prefix = new RegExp('^' + C.NAME + ' / .+');
    //   Object.keys(C).forEach(k => {
    //     expect(k.toUpperCase()).toBe(k);
    //     if (k !== 'NAME') {
    //       expect(C[k]).toMatch(prefix);
    //     }
    //   });
  });
});
